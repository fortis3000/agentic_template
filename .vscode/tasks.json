{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Initialize Environment",
      "detail": "Sync Python (uv) and Node (frontend) dependencies so pre-commit hooks have their tooling.",
      "type": "shell",
      "command": "uv sync --extra all && npm install --prefix frontend",
      "presentation": {
        "reveal": "silent",
        "panel": "shared",
        "clear": true
      },
      "problemMatcher": [],
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
